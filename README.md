### Run application
- Install mobile application **Expo Go** using [this instruction](https://docs.expo.dev/get-started/set-up-your-environment)
- Run `npm start`
- Open **Expo Go** application and can the QR code from console

### Reproduction
```
npx create-expo-app@latest --example with-typescript
```
```
npm install @maskito/core
```